export interface Train {
    id?: number,
    type?: string,
    capacity?: number,
    maxSpeed?: number
}

export interface TrainInput {
    id: number,
    type: string,
    capacity: number,
    maxSpeed: number
}